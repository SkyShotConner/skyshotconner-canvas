import fs from 'node:fs'
const file='app/[[...slug]]/page.tsx'
let s=fs.readFileSync(file,'utf8')
const start=s.indexOf('function Checkout(')
if(start<0) throw new Error('Checkout component not found')
const next=s.indexOf('function ',start+10)
if(next<0) throw new Error('Could not find next component')
const fn=`function Checkout({cart,subtotal,shipping,nav,user,supabase}:{cart:CartItem[];subtotal:number;shipping:number;nav:(x:string)=>void;user:any;supabase:any}){const [form,setForm]=useState({first:'',last:'',email:user?.email||'',phone:'',country:'South Africa',address:'',unit:'',city:'',province:'Gauteng',postal:''});const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');const update=(k:string,v:string)=>setForm({...form,[k]:v});useEffect(()=>{if(user?.email&&!form.email)setForm(f=>({...f,email:user.email}))},[user]);async function submit(e:any){e.preventDefault();setBusy(true);setMsg('');if(!supabase){setMsg('Supabase is not configured yet.');setBusy(false);return}if(!user){setMsg('Please sign in before placing an order.');setBusy(false);return}try{const {data,error}=await supabase.rpc('place_order',{p_customer_email:form.email,p_customer_name:\`${'${form.first}'} ${'${form.last}'}\`,p_customer_phone:form.phone,p_items:cart.map(i=>({product_id:i.product.id,variant_id:null,quantity:i.quantity,unit_price:i.price})),p_shipping_address:{country:form.country,address:form.address,unit:form.unit,city:form.city,province:form.province,postal_code:form.postal}});if(error)throw error;const response=await fetch('/api/payfast/create',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({order_id:String(data)})});const payment=await response.json();if(!response.ok)throw new Error(payment.error||'Could not prepare PayFast payment');const payForm=document.createElement('form');payForm.method='POST';payForm.action=payment.action;Object.entries(payment.fields||{}).forEach(([key,value])=>{const input=document.createElement('input');input.type='hidden';input.name=key;input.value=String(value);payForm.appendChild(input)});document.body.appendChild(payForm);payForm.submit()}catch(err:any){setMsg(err.message||'Could not start payment.')}setBusy(false)}return <main className="page container"><h1 className="page-title">CHECK<br/>OUT.</h1><div className="product-layout"><form className="form" onSubmit={submit}><div className="eyebrow">Customer information</div><div className="form-row"><Field label="First name" value={form.first} set={v=>update('first',v)}/><Field label="Last name" value={form.last} set={v=>update('last',v)}/></div><Field label="Email" value={form.email} set={v=>update('email',v)} type="email"/><Field label="Phone" value={form.phone} set={v=>update('phone',v)}/><div className="eyebrow" style={{marginTop:30}}>Shipping</div><Field label="Country" value={form.country} set={v=>update('country',v)}/><Field label="Address" value={form.address} set={v=>update('address',v)}/><Field label="Apartment / unit" value={form.unit} set={v=>update('unit',v)}/><div className="form-row"><Field label="City" value={form.city} set={v=>update('city',v)}/><Field label="Province / state" value={form.province} set={v=>update('province',v)}/></div><Field label="Postal code" value={form.postal} set={v=>update('postal',v)}/><div className="eyebrow" style={{marginTop:30}}>Delivery</div><div className="notice">Nationwide delivery · R95</div><div className="eyebrow" style={{marginTop:30}}>Payment</div><div className="notice">You will be securely redirected to PayFast to complete your payment.</div>{!user&&<div className="notice">Please sign in before checkout.</div>}{msg&&<div className="notice">{msg}</div>}<button className="primary" disabled={busy||!cart.length||!user}>{busy?'Redirecting to PayFast…':'Continue to PayFast'}</button></form><div><div className="eyebrow">Order summary</div>{cart.map((i,n)=><div className="cart-row" key={n}><img src={imgFor(i.product)} alt=""/><div className="cart-info"><h3>{i.product.name}</h3><p>{i.size} · {i.frame} · {i.quantity}</p></div></div>)}<div className="summary"><div className="summary-line"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="summary-line"><span>Nationwide shipping</span><span>{money(shipping)}</span></div><div className="summary-line total"><span>Total</span><span>{money(subtotal+shipping)}</span></div></div></div></div></main>}`
s=s.slice(0,start)+fn+s.slice(next)
fs.writeFileSync(file,s)
console.log('PayFast checkout wired')
