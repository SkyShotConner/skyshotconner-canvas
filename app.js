const SUPABASE_URL='https://xdkoutuetajwzwrjzehw.supabase.co';
const SUPABASE_KEY='sb_publishable_K0r7cxL94PMJ5S0HM8aKfQ_cc73t2hu';
const CART_KEY='ssc_canvas_cart';
const RETURN_KEY='ssc_return_url';
const SHIPPING=95;
const db=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>`R ${Number(v||0).toLocaleString('en-ZA',{minimumFractionDigits:2})}`;
function cart(){try{return JSON.parse(localStorage.getItem(CART_KEY)||'[]')}catch{return[]}}
function saveCart(c){localStorage.setItem(CART_KEY,JSON.stringify(c));updateCartCount()}
function updateCartCount(){const n=cart().reduce((s,i)=>s+Number(i.qty||1),0);$$('#cartCount').forEach(x=>x.textContent=n)}
function toast(t){let x=$('.toast');if(!x){x=document.createElement('div');x.className='toast';document.body.append(x)}x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),1800)}
function img(path){return path?db.storage.from('product-images').getPublicUrl(path).data.publicUrl:''}
function setupNav(){const b=$('.menu-toggle'),n=$('.main-nav');if(b&&n)b.onclick=()=>n.classList.toggle('open');updateCartCount();const y=$('#year');if(y)y.textContent=new Date().getFullYear()}
function rememberReturn(){sessionStorage.setItem(RETURN_KEY,location.pathname+location.search+location.hash)}
function goLogin(next=''){location.href=`login.html${next?`?next=${encodeURIComponent(next)}`:''}`}
async function requireUser(next=''){const {data:{user}}=await db.auth.getUser();if(!user){goLogin(next||location.href);return null}return user}
async function getProducts({search='',featured=false,category=''}={}){let q=db.from('products').select('*,categories(name),product_images(storage_path,is_primary,sort_order)').eq('is_active',true).order('created_at',{ascending:false});if(featured)q=q.eq('is_featured',true);if(category)q=q.eq('category_id',category);const {data,error}=await q;if(error)throw error;let a=data||[];if(search){const s=search.toLowerCase();a=a.filter(p=>(p.name+' '+(p.short_description||'')+' '+(p.description||'')).toLowerCase().includes(s))}return a.map(p=>({...p,image:img((p.product_images||[]).find(i=>i.is_primary)?.storage_path||(p.product_images||[])[0]?.storage_path)}))}
function productCard(p){return `<article class="product-card"><a class="product-link" href="product.html?id=${encodeURIComponent(p.id)}" onclick="sessionStorage.setItem('${RETURN_KEY}',location.pathname+location.search+location.hash)"><div class="product-image">${p.image?`<img src="${esc(p.image)}" alt="${esc(p.name)}" loading="lazy">`:'✈'}</div><div class="product-info"><div class="product-card-top"><span class="product-badge">${p.is_featured?'FEATURED':'AVIATION CANVAS'}</span><span class="stock-label ${Number(p.stock)>0?'in-stock':'sold-out'}">${Number(p.stock)>0?'IN STOCK':'SOLD OUT'}</span></div><h3>${esc(p.name)}</h3><p>${esc(p.short_description||p.description||'Fine-art aviation photography printed for your wall.')}</p><div class="product-card-meta"><span>${esc(p.categories?.name||'Uncategorised')}</span><span>${esc(p.sku||'No SKU')}</span></div><div class="product-price-row"><span class="price">${money(p.price)}</span>${p.compare_at_price?`<del>${money(p.compare_at_price)}</del>`:''}</div></div></a></article>`}
async function home(){const e=$('#featuredProducts');if(!e)return;try{const a=await getProducts({featured:true});e.innerHTML=a.length?a.slice(0,4).map(productCard).join(''):'<div class="empty"><div class="empty-icon">✈</div><h3>The collection is coming together</h3><p>Your future featured aviation canvases will appear here.</p><a class="btn btn-primary" href="shop.html">Explore shop</a></div>'}catch(err){console.error(err);e.innerHTML='<div class="empty"><h3>Collection unavailable</h3><p>Please refresh and try again.</p></div>'}}
async function shop(){const e=$('#products');if(!e)return;const s=$('#search'),f=$('#categoryFilter');if(f){const {data}=await db.from('categories').select('id,name').eq('is_active',true).order('sort_order');f.innerHTML='<option value="">All collections</option>'+(data||[]).map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}const draw=async()=>{e.innerHTML='<div class="loading-state"><div class="loader-plane">✈</div><p>Loading collection…</p></div>';try{const a=await getProducts({search:s?.value||'',category:f?.value||''});e.innerHTML=a.length?a.map(productCard).join(''):'<div class="empty"><h2>No canvases found</h2><p>Try another collection or search.</p></div>'}catch(err){console.error(err);e.innerHTML='<div class="empty"><h2>Could not load the shop</h2><p>Supabase did not return the catalogue.</p></div>'}};s?.addEventListener('input',()=>{clearTimeout(window.shopTimer);window.shopTimer=setTimeout(draw,250)});f?.addEventListener('change',draw);draw()}
async function product(){const e=$('#product');if(!e)return;const id=new URLSearchParams(location.search).get('id');if(!id){e.innerHTML='<div class="empty"><h2>Product not found</h2><a class="btn btn-primary" href="shop.html">Back to shop</a></div>';return}const {data:p,error}=await db.from('products').select('*,categories(name),product_images(*),product_variants(*)').eq('id',id).eq('is_active',true).single();if(error||!p){e.innerHTML='<div class="empty"><h2>Product not found</h2><a class="btn btn-primary" href="shop.html">Back to shop</a></div>';return}const images=(p.product_images||[]).sort((a,b)=>(a.sort_order||0)-(b.sort_order||0)).map(i=>({...i,url:img(i.storage_path)}));let selected=(p.product_variants||[]).find(v=>v.is_active)||null;let main=images[0]?.url||'';const back=sessionStorage.getItem(RETURN_KEY)||'shop.html';e.innerHTML=`<div class="product-back-row"><button class="btn btn-outline btn-small" id="productBack">← Back</button><span>PRODUCT DETAIL</span></div><div class="product-detail"><div><div class="detail-main-image">${main?`<img id="mainProductImage" src="${esc(main)}" alt="${esc(p.name)}">`:'✈'}</div><div class="thumbs">${images.map(i=>`<button class="thumb" data-src="${esc(i.url)}" type="button"><img src="${esc(i.url)}" alt=""></button>`).join('')}</div></div><div class="detail-copy"><span class="eyebrow">${esc(p.categories?.name||'AVIATION CANVAS')}</span><h1>${esc(p.name)}</h1><p class="detail-description">${esc(p.description||'Premium aviation photography printed as a statement piece.')}</p><div class="detail-price" id="detailPrice">${money(selected?.price??p.price)}</div>${p.compare_at_price?`<div class="compare-price">Normally ${money(p.compare_at_price)}</div>`:''}<label class="field-label">Quantity<input id="qty" type="number" min="1" max="99" value="1"></label>${(p.product_variants||[]).filter(v=>v.is_active).length?`<div class="variant-grid">${p.product_variants.filter(v=>v.is_active).map((v,i)=>`<button class="variant-btn ${i?'':'selected'}" data-id="${v.id}" type="button">${esc(v.name)}<small>${money(v.price??p.price)} · ${Number(v.stock||0)>0?'In stock':'Sold out'}</small></button>`).join('')}</div>`:''}<button id="addProduct" class="btn btn-primary btn-wide" ${((selected&&selected.stock<=0)||(!selected&&p.stock<=0))?'disabled':''}>${((selected&&selected.stock<=0)||(!selected&&p.stock<=0))?'Sold out':'Add to cart'}</button><div class="buy-note">Secure checkout · Account required to purchase</div><div class="product-meta"><span>SKU: ${esc(selected?.sku||p.sku||'—')}</span><span>Nationwide courier delivery: ${money(SHIPPING)}</span></div></div></div>`;$('#productBack').onclick=()=>{sessionStorage.removeItem(RETURN_KEY);location.href=back};document.querySelectorAll('.thumb').forEach(b=>b.onclick=()=>{const m=$('#mainProductImage');if(m)m.src=b.dataset.src});document.querySelectorAll('.variant-btn').forEach(b=>b.onclick=()=>{document.querySelectorAll('.variant-btn').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');selected=p.product_variants.find(v=>v.id===b.dataset.id);$('#detailPrice').textContent=money(selected.price??p.price);$('#addProduct').disabled=selected.stock<=0;$('#addProduct').textContent=selected.stock<=0?'Sold out':'Add to cart'});$('#addProduct')?.addEventListener('click',async()=>{const user=await requireUser();if(!user)return;const qty=Math.max(1,Math.min(99,Number($('#qty').value)||1)),key=`${p.id}:${selected?.id||''}`,c=cart(),old=c.find(i=>i.key===key);if(old)old.qty+=qty;else c.push({key,product_id:p.id,variant_id:selected?.id||null,name:p.name,price:Number(selected?.price??p.price),image:main,qty,sku:selected?.sku||p.sku||''});saveCart(c);toast('Added to cart')})}
function renderCart(){const e=$('#cart');if(!e)return;const c=cart();if(!c.length){e.innerHTML='<div class="empty"><div class="empty-icon">✈</div><h2>Your cart is empty</h2><p>Find an aviation canvas you love and add it here.</p><a class="btn btn-primary" href="shop.html">Shop canvases</a></div>';return}const subtotal=c.reduce((s,i)=>s+Number(i.price||0)*Number(i.qty||1),0),total=subtotal+SHIPPING;e.innerHTML=`<div class="cart-layout"><div class="cart-list"><div class="section-head"><div><span class="eyebrow">YOUR SELECTION</span><h2>Shopping cart</h2><p>${c.length} item${c.length===1?'':'s'} · Courier Guy nationwide delivery</p></div></div>${c.map((i,n)=>`<div class="cart-row"><div class="cart-thumb">${i.image?`<img src="${esc(i.image)}" alt="">`:'✈'}</div><div class="cart-product"><strong>${esc(i.name)}</strong><small>${esc(i.sku||'')}</small><div class="qty-controls"><button type="button" onclick="window.cartQty(${n},-1)">−</button><span>${i.qty}</span><button type="button" onclick="window.cartQty(${n},1)">+</button></div></div><strong>${money(Number(i.price)*Number(i.qty))}</strong><button class="remove-btn" type="button" onclick="window.cartRemove(${n})">Remove</button></div>`).join('')}</div><aside class="cart-total"><div><span>Subtotal</span><strong>${money(subtotal)}</strong></div><div><span>Courier Guy delivery</span><strong>${money(SHIPPING)}</strong></div><div class="grand-total"><span>Total</span><strong>${money(total)}</strong></div><a class="btn btn-primary btn-wide" href="checkout.html">Proceed to checkout</a><a class="btn btn-outline btn-wide" href="shop.html" style="margin-top:9px">Continue shopping</a><p class="checkout-gate">You must have a SkyShotConner account to purchase.</p></aside></div>`}
window.cartQty=(i,d)=>{const c=cart();if(!c[i])return;c[i].qty=Math.max(1,Math.min(99,Number(c[i].qty||1)+d));saveCart(c);renderCart()};window.cartRemove=i=>{const c=cart();c.splice(i,1);saveCart(c);renderCart()};
async function account(){const e=$('#accountBox');if(!e)return;const {data:{user}}=await db.auth.getUser();if(!user){e.innerHTML=`<div class="account-guest panel"><span class="eyebrow">CUSTOMER ACCOUNT</span><h1>Your account is waiting.</h1><p>Create an account or sign in to manage your profile, orders and purchases.</p><div class="actions"><a class="btn btn-primary" href="login.html">Sign in</a><a class="btn btn-outline" href="signup.html">Create account</a></div></div>`;return}const {data:profile}=await db.from('profiles').select('*').eq('id',user.id).maybeSingle();const {data:orders}=await db.from('orders').select('*').eq('user_id',user.id).order('created_at',{ascending:false});const name=profile?.full_name||user.user_metadata?.full_name||user.email.split('@')[0];e.innerHTML=`<div class="account-head"><div><span class="eyebrow">YOUR ACCOUNT</span><h1>Welcome, ${esc(name)}.</h1><p>${esc(user.email)}</p></div><button id="signOut" class="btn btn-outline">Sign out</button></div><div class="account-grid"><section class="panel"><span class="eyebrow">PROFILE</span><h2>Your details</h2><form id="profileForm"><input name="full_name" value="${esc(profile?.full_name||name)}" placeholder="Full name"><input name="phone" value="${esc(profile?.phone||'')}" placeholder="Phone number"><button class="btn btn-primary">Save details</button><p id="profileMsg" class="form-msg"></p></form></section><section class="panel account-help"><span class="eyebrow">SHOPPING</span><h2>Ready to fly?</h2><p>Your delivery address is collected securely at checkout only. Nationwide Courier Guy delivery is ${money(SHIPPING)}.</p><a class="btn btn-primary" href="shop.html">Browse canvases</a></section></div><section class="orders-panel"><span class="eyebrow">ORDER HISTORY</span><h2>Your orders</h2>${orders?.length?orders.map(o=>`<div class="order-row"><div><strong>#${o.id.slice(0,8).toUpperCase()}</strong><span>${new Date(o.created_at).toLocaleDateString('en-ZA')}</span></div><span class="status-pill">${esc(o.status)} · ${esc(o.payment_status)}</span><strong>${money(o.total)}</strong></div>`).join(''):'<p>No orders yet.</p>'}</section>`;$('#signOut').onclick=async()=>{await db.auth.signOut();location.reload()};$('#profileForm').onsubmit=async ev=>{ev.preventDefault();const f=new FormData(ev.currentTarget),r=await db.from('profiles').upsert({id:user.id,full_name:f.get('full_name'),phone:f.get('phone')});$('#profileMsg').textContent=r.error?r.error.message:'Details saved.'}}
async function checkout(){const e=$('#checkoutApp');if(!e)return;const c=cart();if(!c.length){e.innerHTML='<div class="empty"><h2>Your cart is empty</h2><a class="btn btn-primary" href="shop.html">Shop canvases</a></div>';return}const user=await requireUser('checkout.html');if(!user)return;const {data:profile}=await db.from('profiles').select('*').eq('id',user.id).maybeSingle();const name=profile?.full_name||user.user_metadata?.full_name||user.email.split('@')[0];const subtotal=c.reduce((s,i)=>s+Number(i.price)*Number(i.qty),0);e.innerHTML=`<div class="checkout-layout"><section class="panel"><div class="checkout-head"><div><span class="eyebrow">SECURE CHECKOUT</span><h1>Delivery details</h1><p>Your address is used only for this order.</p></div><a class="btn btn-outline btn-small" href="cart.html">← Cart</a></div><form id="checkoutForm"><input required name="address" placeholder="Street address"><div class="form-grid-2"><input required name="city" placeholder="City"><input required name="province" placeholder="Province"></div><input required name="postal" placeholder="Postal code"><textarea name="notes" rows="4" placeholder="Order notes (optional)"></textarea><div class="shipping-callout"><strong>Courier Guy nationwide delivery</strong><span>${money(SHIPPING)} flat rate</span></div><button class="btn btn-primary btn-wide" type="submit">Place order</button><p id="checkoutMsg" class="form-msg"></p></form></section><aside class="checkout-summary panel"><span class="eyebrow">ORDER SUMMARY</span><h2>${c.length} item${c.length===1?'':'s'}</h2>${c.map(i=>`<div class="summary-item"><span>${esc(i.name)} × ${i.qty}</span><strong>${money(Number(i.price)*Number(i.qty))}</strong></div>`).join('')}<div class="summary-line"><span>Subtotal</span><strong>${money(subtotal)}</strong></div><div class="summary-line"><span>Courier Guy</span><strong>${money(SHIPPING)}</strong></div><div class="summary-grand"><span>Total</span><strong>${money(subtotal+SHIPPING)}</strong></div><p>Purchasing as ${esc(name)} · ${esc(user.email)}</p></aside></div>`;$('#checkoutForm').onsubmit=async ev=>{ev.preventDefault();const m=$('#checkoutMsg'),btn=ev.currentTarget.querySelector('button[type=submit]');btn.disabled=true;m.textContent='Creating order…';const f=new FormData(ev.currentTarget),r=await db.rpc('place_order',{p_items:c.map(i=>({product_id:i.product_id,variant_id:i.variant_id,quantity:i.qty})),p_shipping_address:{address:f.get('address'),city:f.get('city'),province:f.get('province'),postal_code:f.get('postal'),notes:f.get('notes')},p_customer_name:name,p_customer_email:user.email,p_customer_phone:profile?.phone||null});if(r.error){m.textContent=r.error.message;btn.disabled=false;return}localStorage.removeItem(CART_KEY);updateCartCount();e.innerHTML=`<div class="empty"><div class="empty-icon">✓</div><h1>Order received</h1><p>Your order <strong>#${String(r.data).slice(0,8).toUpperCase()}</strong> has been created successfully.</p><p>Courier Guy delivery: ${money(SHIPPING)} nationwide. We'll process the order once payment is confirmed.</p><a class="btn btn-primary" href="account.html">View account</a></div>`}}
async function authPage(mode){const e=$('#authPage');if(!e)return;const {data:{user}}=await db.auth.getUser();if(user){location.href=new URLSearchParams(location.search).get('next')||'account.html';return}const login=mode==='login';e.innerHTML=`<div class="auth-page-shell"><div class="auth-visual"><span class="eyebrow">SKYSHOTCONNER CANVAS</span><h1>${login?'Welcome back.':'Start your collection.'}</h1><p>${login?'Sign in to manage your account and purchase aviation photography.':'Create your customer account and start collecting aviation photography.'}</p><div class="auth-flightline"><span>01</span><i></i><span>SSC</span></div></div><div class="auth-form-panel"><a class="auth-brand" href="index.html">✈ SkyShotConner <b>Canvas</b></a><span class="eyebrow">${login?'CUSTOMER LOGIN':'CUSTOMER SIGNUP'}</span><h2>${login?'Sign in':'Create your account'}</h2><p class="auth-sub">${login?'Access your orders and continue shopping.':'Only registered customers can purchase.'}</p><form id="authForm">${login?'':'<label>Full name<input required name="name" autocomplete="name" placeholder="Your full name"></label>'}<label>Email<input required name="email" type="email" autocomplete="email" placeholder="you@example.com"></label><label>Password<input required name="password" minlength="6" type="password" autocomplete="current-password" placeholder="Minimum 6 characters"></label><button class="btn btn-primary btn-wide" type="submit">${login?'Sign in':'Create account'}</button><p id="authMsg" class="form-msg"></p></form><div class="auth-switch">${login?`New here? <a href="signup.html">Create an account</a>`:`Already have an account? <a href="login.html">Sign in</a>`}</div><a class="auth-home" href="index.html">← Back to store</a></div></div>`;$('#authForm').onsubmit=async ev=>{ev.preventDefault();const f=new FormData(ev.currentTarget),m=$('#authMsg'),btn=ev.currentTarget.querySelector('button');btn.disabled=true;m.textContent=login?'Signing in…':'Creating account…';let r;if(login)r=await db.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});else r=await db.auth.signUp({email:f.get('email'),password:f.get('password'),options:{data:{full_name:f.get('name')}}});if(r.error){m.textContent=r.error.message;btn.disabled=false;return}const next=new URLSearchParams(location.search).get('next');if(r.data.session)location.href=next||'account.html';else m.textContent='Account created. Check your email to confirm your account.'}}
setupNav();
if(location.pathname.endsWith('index.html')||location.pathname.endsWith('/'))home();
if(location.pathname.endsWith('shop.html'))shop();
if(location.pathname.endsWith('product.html'))product();
if(location.pathname.endsWith('cart.html'))renderCart();
if(location.pathname.endsWith('account.html'))account();
if(location.pathname.endsWith('checkout.html'))checkout();