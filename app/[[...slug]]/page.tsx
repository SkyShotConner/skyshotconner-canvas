'use client'

import { useEffect, useMemo, useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Heart, Search, ShoppingBag, User, ArrowUpRight, Menu, X, Trash2, Minus, Plus, ChevronDown } from 'lucide-react'
import { createClient, storageUrl } from '@/lib/supabase/client'

const FALLBACK = 'https://images.unsplash.com/photo-1542296332-2e4473faf563?auto=format&fit=crop&w=1800&q=85'
const editorial = {
  commercial:'https://images.unsplash.com/photo-1436491865332-7a61a109cc05?auto=format&fit=crop&w=1800&q=85',
  military:'https://images.unsplash.com/photo-1559628233-2a5d6c5e6a2c?auto=format&fit=crop&w=1800&q=85',
  historic:'https://images.unsplash.com/photo-1529070538774-1843cb3265df?auto=format&fit=crop&w=1800&q=85',
  cockpit:'https://images.unsplash.com/photo-1517479149777-5f3b1511d5ad?auto=format&fit=crop&w=1800&q=85'
}

type Product={id:string;name:string;slug:string;price:number;short_description?:string|null;description?:string|null;category?:string|null;images:string[]}
type CartItem={product:Product;size:string;frame:string;quantity:number;price:number}

const demoProducts:Product[]=[
 {id:'demo-747',name:'BOEING 747 — QUEEN OF THE SKIES',slug:'boeing-747-queen-of-the-skies',price:1899,category:'Commercial',short_description:'The silhouette that defined an era.',images:['https://images.unsplash.com/photo-1436491865332-7a61a109cc05?auto=format&fit=crop&w=1400&q=85']},
 {id:'demo-harvard',name:'HARVARD — GOLDEN HOUR',slug:'harvard-golden-hour',price:2199,category:'Historic',short_description:'A classic trainer suspended in sunset.',images:[FALLBACK]},
 {id:'demo-concorde',name:'CONCORDE — MACH 2',slug:'concorde-mach-2',price:2499,category:'Historic',short_description:'Speed, elegance, engineering.',images:['https://images.unsplash.com/photo-1540962351504-03099e0a754b?auto=format&fit=crop&w=1400&q=85']},
 {id:'demo-cockpit',name:'FLIGHT DECK — ABOVE THE CLOUDS',slug:'flight-deck-above-the-clouds',price:1699,category:'Cockpit',short_description:'The view from where aviation begins.',images:[editorial.cockpit]}
]

function imgFor(p:Product){ return p.images?.[0]?.startsWith('http')?p.images[0]:storageUrl(p.images?.[0]||'')||FALLBACK }
function money(n:number){return new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:0}).format(n)}

export default function Site(){
 const [path,setPath]=useState(typeof window!=='undefined'?window.location.pathname:'/')
 const [menu,setMenu]=useState(false),[searchOpen,setSearchOpen]=useState(false),[query,setQuery]=useState('')
 const [products,setProducts]=useState<Product[]>(demoProducts),[cart,setCart]=useState<CartItem[]>([]),[wish,setWish]=useState<string[]>([])
 const [selectedSize,setSelectedSize]=useState('Medium'),[selectedFrame,setSelectedFrame]=useState('Black')
 const [user,setUser]=useState<any>(null)
 const supabase=useMemo(()=>createClient(),[])
 useEffect(()=>{const on=()=>setPath(window.location.pathname);window.addEventListener('popstate',on);return()=>window.removeEventListener('popstate',on)},[])
 useEffect(()=>{try{setCart(JSON.parse(localStorage.getItem('ssc-cart')||'[]'));setWish(JSON.parse(localStorage.getItem('ssc-wish')||'[]'))}catch{}}
 ,[])
 useEffect(()=>{localStorage.setItem('ssc-cart',JSON.stringify(cart))},[cart]);useEffect(()=>{localStorage.setItem('ssc-wish',JSON.stringify(wish))},[wish])
 useEffect(()=>{if(!supabase)return;supabase.auth.getUser().then(({data})=>setUser(data.user));const {data}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));return()=>data.subscription.unsubscribe()},[supabase])
 useEffect(()=>{if(!supabase)return;supabase.from('products').select('id,name,slug,price,short_description,description,category_id,product_images(storage_path)').eq('is_active',true).then(({data})=>{if(data?.length){setProducts(data.map((p:any)=>({...p,category:p.category_id,images:(p.product_images||[]).map((x:any)=>x.storage_path)})))}})},[supabase])
 const nav=(to:string)=>{window.history.pushState({},'',to);setPath(to);setMenu(false);window.scrollTo(0,0)}
 const add=(p:Product)=>setCart(c=>{const key=p.id+selectedSize+selectedFrame;const hit=c.find(x=>x.product.id+ x.size+x.frame===key);return hit?c.map(x=>x===hit?{...x,quantity:x.quantity+1}:x):[...c,{product:p,size:selectedSize,frame:selectedFrame,quantity:1,price:p.price+(selectedSize==='Large'?400:selectedSize==='Extra Large'?800:0)+(selectedFrame==='Black'?150:selectedFrame==='White'?100:selectedFrame==='Natural Wood'?250:0)}]})
 const filtered=products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(query.toLowerCase()))
 const currentSlug=path.startsWith('/product/')?path.split('/')[2]:'';const current=products.find(p=>p.slug===currentSlug)||demoProducts.find(p=>p.slug===currentSlug)
 const subtotal=cart.reduce((s,i)=>s+i.price*i.quantity,0),shipping=subtotal?subtotal>3000?0:180:0
 return <div>
  <Nav cart={cart.length} onMenu={()=>setMenu(true)} onSearch={()=>setSearchOpen(true)} onNav={nav} user={!!user}/>
  <AnimatePresence>{menu&&<MobileMenu close={()=>setMenu(false)} nav={nav}/>}</AnimatePresence>
  <AnimatePresence>{searchOpen&&<SearchOverlay close={()=>setSearchOpen(false)} query={query} setQuery={setQuery} products={filtered} nav={nav}/>}</AnimatePresence>
  {path==='/'?<Home nav={nav} products={products}/>:path==='/shop'||path.startsWith('/collections')?<Shop products={filtered} nav={nav} query={query} setQuery={setQuery}/>:path.startsWith('/product/')?<ProductPage product={current||demoProducts[0]} selectedSize={selectedSize} setSelectedSize={setSelectedSize} selectedFrame={selectedFrame} setSelectedFrame={setSelectedFrame} add={add} wished={wish.includes(current?.id||'')} toggleWish={()=>setWish(w=>w.includes(current?.id||'')?w.filter(x=>x!==current?.id):[...w,current?.id||''])}/>:path==='/cart'?<Cart cart={cart} setCart={setCart} nav={nav} subtotal={subtotal} shipping={shipping}/>:path==='/checkout'?<Checkout cart={cart} subtotal={subtotal} shipping={shipping} nav={nav} user={user} supabase={supabase}/>:path==='/wishlist'?<Wishlist products={products.filter(p=>wish.includes(p.id))} nav={nav} add={add}/>:path==='/search'?<SearchPage products={filtered} query={query} setQuery={setQuery} nav={nav}/>:path==='/login'?<Auth mode="login" nav={nav} supabase={supabase}/>:path==='/signup'?<Auth mode="signup" nav={nav} supabase={supabase}/>:path==='/forgot-password'?<Auth mode="forgot" nav={nav} supabase={supabase}/>:path.startsWith('/account')?<Account user={user} nav={nav} supabase={supabase}/>:path==='/about'?<About/>:path==='/journal'||path.startsWith('/journal/')?<Journal article={path!=='/journal'} nav={nav}/>:<SimplePage title={path.slice(1).replaceAll('-',' ')} />}
  <Footer nav={nav}/>
 </div>
}

function Nav({cart,onMenu,onSearch,onNav,user}:{cart:number;onMenu:()=>void;onSearch:()=>void;onNav:(x:string)=>void;user:boolean}){return <header className="site-nav"><button className="iconbtn mobile-only" onClick={onMenu}><Menu size={17}/></button><button className="brand" onClick={()=>onNav('/')}>SKYSHOTCONNER</button><nav className="navlinks"><button onClick={()=>onNav('/shop')}>Collection</button><button onClick={()=>onNav('/collections/commercial')}>Aircraft</button><button onClick={()=>onNav('/journal')}>Journal</button><button onClick={()=>onNav('/about')}>About</button></nav><div className="navtools"><button onClick={onSearch}><Search size={14}/></button><button onClick={()=>onNav(user?'/account':'/login')}><User size={14}/></button><button className="optional" onClick={()=>onNav('/wishlist')}><Heart size={14}/></button><button onClick={()=>onNav('/cart')}><ShoppingBag size={14}/><span>{cart}</span></button></div></header>}
function MobileMenu({close,nav}:{close:()=>void;nav:(x:string)=>void}){return <motion.div className="fixed inset-0 z-[60] bg-[#0a0a09] p-6" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><div className="flex justify-between"><span className="brand">SKYSHOTCONNER</span><button onClick={close}><X/></button></div><div className="flex flex-col gap-8 mt-24 text-4xl font-light"><button onClick={()=>nav('/shop')}>Collection</button><button onClick={()=>nav('/collections/commercial')}>Aircraft</button><button onClick={()=>nav('/journal')}>Journal</button><button onClick={()=>nav('/about')}>About</button><button onClick={()=>nav('/account')}>Account</button></div></motion.div>}
function Home({nav,products}:{nav:(x:string)=>void;products:Product[]}){return <main><section className="hero"><img className="hero-img" src={FALLBACK} alt="Harvard aircraft turning through golden sunset"/><div className="hero-copy"><div className="eyebrow">SkyShotConner / Aviation Art</div><h1 className="display">THE ART<br/>OF FLIGHT.</h1><p>Premium aviation canvas artwork for those who live to fly.</p><button className="hero-cta" onClick={()=>nav('/shop')}>Explore the collection <ArrowUpRight size={14}/></button></div><div className="scroll-note">Scroll to fly</div></section><Scene title="C O M M E R C I A L" image={editorial.commercial} eyebrow="01 / Modern aviation" nav={nav}/><Scene title="M I L I T A R Y" image={editorial.military} eyebrow="02 / Power & precision" nav={nav}/><Scene title="H I S T O R I C" image={editorial.historic} eyebrow="03 / Aviation heritage" nav={nav}/><Scene title="C O C K P I T" image={editorial.cockpit} eyebrow="04 / Where flight begins" nav={nav}/><section className="paper"><div className="container"><div className="editorial"><img src={FALLBACK} alt="Classic aircraft in cinematic light"/><div className="editorial-copy"><div className="eyebrow">The collection</div><h2>Aircraft are not just machines. They are stories.</h2><p>Every SkyShotConner canvas begins with a moment in flight — frozen, refined and made tangible. From airliners to warbirds, each piece is photographed with the intention of becoming art.</p><button className="hero-cta" onClick={()=>nav('/shop')}>View artworks <ArrowUpRight size={14}/></button></div></div><div style={{height:120}}/><div className="section-head"><div><div className="eyebrow">Selected works</div><h2 style={{fontSize:'clamp(35px,5vw,72px)',fontWeight:300,margin:'12px 0'}}>Icons of aviation.</h2></div><button className="eyebrow" onClick={()=>nav('/shop')}>View all →</button></div><div className="products">{products.slice(0,3).map((p,i)=><ProductCard key={p.id} p={p} nav={nav} small={i>0}/>)}</div></div></section></main>}
function Scene({title,image,eyebrow,nav}:{title:string;image:string;eyebrow:string;nav:(x:string)=>void}){return <section className="scene"><img src={image} alt="Aviation artwork" loading="lazy"/><div className="lens"><span>VIEW COLLECTION</span></div><div className="scene-copy"><div className="eyebrow">{eyebrow}</div><button className="wide" onClick={()=>nav('/shop')}>{title}</button></div></section>}
function ProductCard({p,nav,small=false}:{p:Product;nav:(x:string)=>void;small?:boolean}){return <button className={'product-card '+(small?'small':'')} onClick={()=>nav('/product/'+p.slug)}><img src={imgFor(p)} alt={p.name} loading="lazy"/><div className="product-meta"><div className="product-name">{p.name}</div><div className="product-sub">{p.category||'Aviation Collection'}</div><div className="price">From {money(p.price)}</div></div></button>}
function Shop({products,nav,query,setQuery}:{products:Product[];nav:(x:string)=>void;query:string;setQuery:(x:string)=>void}){return <main className="page container"><h1 className="page-title">THE<br/>COLLECTION.</h1><div className="toolbar"><span>{products.length} artworks</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="SEARCH" style={{background:'transparent',border:0,color:'inherit',outline:0,textAlign:'right',textTransform:'uppercase',letterSpacing:'.18em',fontSize:10}}/><span>Filter <ChevronDown size={12} style={{verticalAlign:'middle'}}/></span></div><div className="shop-grid">{products.map(p=><ProductCard key={p.id} p={p} nav={nav}/>)}</div></main>}
function ProductPage({product,picked,selectedSize,setSelectedSize,selectedFrame,setSelectedFrame,add,wished,toggleWish}:{product:Product;picked?:string;selectedSize:string;setSelectedSize:(x:string)=>void;selectedFrame:string;setSelectedFrame:(x:string)=>void;add:(p:Product)=>void;wished:boolean;toggleWish:()=>void}){return <main className="product-page container"><div className="product-layout"><div className="gallery">{[...product.images,...product.images].slice(0,3).map((x,i)=><img key={i} src={x.startsWith('http')?x:storageUrl(x)} alt={product.name}/>)}</div><div className="product-info"><div className="eyebrow">{product.category||'Aviation Art'}</div><h1>{product.name}</h1><div className="price">From {money(product.price)}</div><p>{product.description||product.short_description||'A cinematic aviation photograph, printed on premium canvas and prepared for display.'}</p><div className="options"><div className="option-title">Size</div><div className="option-row">{['Small','Medium','Large','Extra Large'].map(x=><button className={'option '+(selectedSize===x?'active':'')} onClick={()=>setSelectedSize(x)} key={x}>{x}</button>)}</div><div className="option-title">Frame</div><div className="option-row">{['Unframed','Black','White','Natural Wood'].map(x=><button className={'option '+(selectedFrame===x?'active':'')} onClick={()=>setSelectedFrame(x)} key={x}>{x}</button>)}</div></div><button className="primary" onClick={()=>add(product)}>Add to bag</button><button className="secondary" onClick={toggleWish}>{wished?'Remove from wishlist':'Save to wishlist'} <Heart size={13} style={{verticalAlign:'middle'}}/></button><div className="notice" style={{marginTop:20}}>Made to order. Secure checkout via PayFast when payment configuration is enabled.</div></div></div></main>}
function Cart({cart,setCart,nav,subtotal,shipping}:{cart:CartItem[];setCart:(x:CartItem[])=>void;nav:(x:string)=>void;subtotal:number;shipping:number}){return <main className="page container"><h1 className="page-title">YOUR<br/>BAG.</h1>{!cart.length?<div className="notice">Your bag is empty. <button onClick={()=>nav('/shop')}><u>Explore the collection.</u></button></div>:<><div>{cart.map((i,idx)=><div className="cart-row" key={idx}><img src={imgFor(i.product)} alt=""/><div className="cart-info"><h3>{i.product.name}</h3><p>{i.size} · {i.frame} · Qty {i.quantity}</p><p>{money(i.price)}</p></div><div className="cart-actions"><button className="iconbtn" onClick={()=>setCart(cart.map((x,j)=>j===idx?{...x,quantity:Math.max(1,x.quantity-1)}:x))}><Minus size={14}/></button><span>{i.quantity}</span><button className="iconbtn" onClick={()=>setCart(cart.map((x,j)=>j===idx?{...x,quantity:x.quantity+1}:x))}><Plus size={14}/></button><button className="iconbtn" onClick={()=>setCart(cart.filter((_,j)=>j!==idx))}><Trash2 size={14}/></button></div></div>)}</div><div className="summary"><div className="summary-line"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="summary-line"><span>Shipping</span><span>{shipping?'R 180':'Complimentary'}</span></div><div className="summary-line total"><span>Total</span><span>{money(subtotal+shipping)}</span></div><button className="primary" onClick={()=>nav('/checkout')}>Proceed to checkout</button></div></>}</main>}
function Checkout({cart,subtotal,shipping,nav,user,supabase}:{cart:CartItem[];subtotal:number;shipping:number;nav:(x:string)=>void;user:any;supabase:any}){const [form,setForm]=useState({first:'',last:'',email:user?.email||'',phone:'',country:'South Africa',address:'',unit:'',city:'',province:'Gauteng',postal:''});const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');const update=(k:string,v:string)=>setForm({...form,[k]:v});async function submit(e:any){e.preventDefault();setBusy(true);setMsg('');if(!supabase){setMsg('Supabase is not configured yet. Add the public project URL and publishable key to enable orders.');setBusy(false);return}try{const {data,error}=await supabase.rpc('place_order',{p_customer_email:form.email,p_customer_name:`${form.first} ${form.last}`,p_customer_phone:form.phone,p_items:cart.map(i=>({product_id:i.product.id,variant_id:null,quantity:i.quantity,unit_price:i.price})),p_shipping_address:{...form}});if(error)throw error;localStorage.removeItem('ssc-cart');nav('/order-confirmation?order='+data)}catch(err:any){setMsg(err.message||'Could not create the order.')}setBusy(false)}return <main className="page container"><h1 className="page-title">CHECK<br/>OUT.</h1><div className="product-layout"><form className="form" onSubmit={submit}><div className="eyebrow">Customer information</div><div className="form-row"><Field label="First name" value={form.first} set={v=>update('first',v)}/><Field label="Last name" value={form.last} set={v=>update('last',v)}/></div><Field label="Email" value={form.email} set={v=>update('email',v)} type="email"/><Field label="Phone" value={form.phone} set={v=>update('phone',v)}/><div className="eyebrow" style={{marginTop:30}}>Shipping</div><Field label="Country" value={form.country} set={v=>update('country',v)}/><Field label="Address" value={form.address} set={v=>update('address',v)}/><Field label="Apartment / unit" value={form.unit} set={v=>update('unit',v)}/><div className="form-row"><Field label="City" value={form.city} set={v=>update('city',v)}/><Field label="Province / state" value={form.province} set={v=>update('province',v)}/></div><Field label="Postal code" value={form.postal} set={v=>update('postal',v)}/><div className="eyebrow" style={{marginTop:30}}>Delivery</div><div className="notice">Standard delivery · R180 · Express delivery can be enabled in the shipping service layer.</div><div className="eyebrow" style={{marginTop:30}}>Payment</div><div className="notice">PayFast is the intended payment provider. Payment credentials and server configuration are required before live payment processing is enabled.</div>{msg&&<div className="notice">{msg}</div>}<button className="primary" disabled={busy||!cart.length}>{busy?'Preparing order…':'Continue to PayFast'}</button></form><div><div className="eyebrow">Order summary</div>{cart.map((i,n)=><div className="cart-row" key={n}><img src={imgFor(i.product)} alt=""/><div className="cart-info"><h3>{i.product.name}</h3><p>{i.size} · {i.frame} · {i.quantity}</p></div></div>)}<div className="summary"><div className="summary-line"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="summary-line"><span>Shipping</span><span>{money(shipping)}</span></div><div className="summary-line total"><span>Total</span><span>{money(subtotal+shipping)}</span></div></div></div></div></main>}
function Field({label,value,set,type='text'}:{label:string;value:string;set:(v:string)=>void;type?:string}){return <div className="field"><label>{label}</label><input required={label!=='Apartment / unit'} type={type} value={value} onChange={e=>set(e.target.value)}/></div>}
function Auth({mode,nav,supabase}:{mode:'login'|'signup'|'forgot';nav:(x:string)=>void;supabase:any}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();if(!supabase){setMsg('Connect Supabase with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY first.');return}let r;if(mode==='login')r=await supabase.auth.signInWithPassword({email,password});else if(mode==='signup')r=await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});else r=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/account'});if(r.error)setMsg(r.error.message);else{setMsg(mode==='forgot'?'Check your email for the reset link.':'Success.');if(mode==='login')nav('/account')}}return <main className="page container"><div className="form-wrap"><div className="eyebrow">SkyShotConner</div><h1 className="page-title">{mode==='login'?'WELCOME BACK':mode==='signup'?'JOIN THE FLIGHT':'RESET ACCESS'}</h1><form className="form" onSubmit={go}>{mode==='signup'&&<Field label="Full name" value={name} set={setName}/>}<Field label="Email" value={email} set={setEmail} type="email"/>{mode!=='forgot'&&<Field label="Password" value={password} set={setPassword} type="password"/>}{msg&&<div className="notice">{msg}</div>}<button className="primary">{mode==='login'?'Sign in':mode==='signup'?'Create account':'Send reset link'}</button></form><div style={{display:'flex',justifyContent:'space-between',marginTop:22,fontSize:11,color:'#999'}}>{mode==='login'?<><button onClick={()=>nav('/forgot-password')}>Forgot password?</button><button onClick={()=>nav('/signup')}>Create account</button></>:<button onClick={()=>nav('/login')}>Already have an account? Sign in</button>}</div></div></main>}
function Wishlist({products,nav,add}:{products:Product[];nav:(x:string)=>void;add:(p:Product)=>void}){return <main className="page container"><h1 className="page-title">SAVED<br/>WORKS.</h1>{products.length?<div className="shop-grid">{products.map(p=><div key={p.id}><ProductCard p={p} nav={nav}/><button className="secondary" onClick={()=>add(p)}>Add to cart</button></div>)}</div>:<div className="notice">Your wishlist is waiting for its first piece.</div>}</main>}
function SearchPage({products,query,setQuery,nav}:{products:Product[];query:string;setQuery:(x:string)=>void;nav:(x:string)=>void}){return <main className="search-page"><div className="eyebrow">Search the collection</div><input className="search-input" autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Boeing 747, Concorde…"/><div className="shop-grid" style={{marginTop:70}}>{products.map(p=><ProductCard key={p.id} p={p} nav={nav}/>)}</div></main>}
function SearchOverlay({close,query,setQuery,products,nav}:{close:()=>void;query:string;setQuery:(x:string)=>void;products:Product[];nav:(x:string)=>void}){return <motion.div className="fixed inset-0 z-[70] bg-[#0a0a09] p-6 md:p-14 overflow-auto" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><div className="flex justify-between"><span className="eyebrow">Search the collection</span><button onClick={close}><X/></button></div><input className="search-input mt-24" autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Boeing 747…"/><div className="grid md:grid-cols-3 gap-8 mt-12">{products.slice(0,6).map(p=><ProductCard key={p.id} p={p} nav={nav}/>)}</div></motion.div>}
function Account({user,nav,supabase}:{user:any;nav:(x:string)=>void;supabase:any}){return <main className="page container"><div className="eyebrow">Account</div><h1 className="page-title">{user?'YOUR<br/>FLIGHT LOG.':'SIGN IN TO<br/>CONTINUE.'}</h1>{user?<div className="grid md:grid-cols-3 gap-4"><button className="notice text-left" onClick={()=>nav('/account/orders')}>Orders<br/><small>View your purchases</small></button><button className="notice text-left" onClick={()=>nav('/wishlist')}>Wishlist<br/><small>Saved artworks</small></button><button className="notice text-left" onClick={async()=>{await supabase.auth.signOut();location.reload()}}>Logout<br/><small>End session</small></button></div>:<button className="primary max-w-md" onClick={()=>nav('/login')}>Sign in</button>}</main>}
function About(){return <main><section className="hero" style={{minHeight:'75svh'}}><img className="hero-img" src={editorial.historic} alt="Aviation heritage"/><div className="hero-copy"><div className="eyebrow">About the collection</div><h1 className="display">WHY<br/>WE FLY.</h1></div></section><section className="paper"><div className="container editorial"><div className="editorial-copy"><div className="eyebrow">The photography</div><h2>Moments become objects.</h2><p>SkyShotConner turns the split second of an aircraft in light, motion and atmosphere into collectible wall art. The aim is simple: make aviation feel as powerful in a room as it feels in the sky.</p></div><img src={FALLBACK} alt="Aircraft photography"/></div></section></main>}
function Journal({article,nav}:{article:boolean;nav:(x:string)=>void}){const articles=[['The Story of the Boeing 747','Aviation history','The aircraft that changed the scale of flight.'],['Why Concorde Still Matters','Engineering','Speed, elegance and the impossible made real.'],['Inside the Cockpit','Flight','Where every journey begins.'],['The Golden Age of Aviation','Heritage','A visual journey through aviation history.']];return <main className="page container"><div className="eyebrow">Aviation Journal</div><h1 className="page-title">THE<br/>JOURNAL.</h1>{article?<div className="form-wrap"><div className="eyebrow">Engineering / 01</div><h2 style={{fontSize:'clamp(40px,7vw,90px)',fontWeight:300,lineHeight:.95}}>Why Concorde Still Matters.</h2><p style={{color:'#999',lineHeight:1.9,fontSize:14}}>Some aircraft become transportation. Others become cultural symbols. Concorde belongs to the second group — an engineering statement that made the future visible from the runway.</p></div>:<div className="shop-grid">{articles.map((a,i)=><button key={i} className="product-card" onClick={()=>nav('/journal/'+i)}><img src={[editorial.historic,editorial.commercial,FALLBACK,editorial.military][i]} alt=""/><div className="product-meta"><div className="eyebrow">{a[1]}</div><div className="product-name" style={{marginTop:8}}>{a[0]}</div><div className="product-sub">{a[2]}</div></div></button>)}</div>}</main>}
function SimplePage({title}:{title:string}){return <main className="page container"><h1 className="page-title">{title.toUpperCase()}.</h1><div className="notice">This editorial page is ready for its final content and can be managed through the future admin system.</div></main>}
function Footer({nav}:{nav:(x:string)=>void}){return <footer className="footer"><div className="container footer-grid"><div><div className="brand">SKYSHOTCONNER</div><p style={{maxWidth:300}}>Aviation captured as art. Premium canvas pieces made for people who look up.</p></div><div><h3>Explore</h3><button onClick={()=>nav('/shop')}>Collection</button><br/><button onClick={()=>nav('/journal')}>Journal</button><br/><button onClick={()=>nav('/about')}>About</button></div><div><h3>Help</h3><button onClick={()=>nav('/contact')}>Contact</button><br/><button onClick={()=>nav('/faq')}>FAQ</button><br/><button onClick={()=>nav('/shipping-returns')}>Shipping</button></div><div><h3>Legal</h3><button onClick={()=>nav('/privacy')}>Privacy</button><br/><button onClick={()=>nav('/terms')}>Terms</button></div></div><div className="container" style={{marginTop:70,borderTop:'1px solid #242421',paddingTop:20}}><p>© {new Date().getFullYear()} SkyShotConner. The Art of Flight.</p></div></footer>}
